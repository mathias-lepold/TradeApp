# TradeApp Frontend — lokale Entwicklung
# Diese Datei wird von .gitignore ignoriert (nie ins Git einchecken!)

# Backend API URL (FastAPI läuft lokal auf Port 8000)
VITE_API_URL=http://localhost:8000

# WebSocket URL für Live-Kurse
VITE_WS_URL=ws://localhost:8000

# App-Name (optional, für UI-Anzeige)
VITE_APP_NAME=TradeApp
